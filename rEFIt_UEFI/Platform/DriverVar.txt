Driver#### structure  L"Driver%04x"

struct {
UINT32 Attribute;
UINT16 FilePathSize;
CHAR16 Description[N];  //ptr += StrSize(Ptr)
*(EFI_DEVICE_PATH_PROTOCOL*) DevicePath; //ptr =+ FilePathSize;
UINT8 LoadOptions[LoadOptionsSize];

}

typedef struct {

  UINTN                     Signature;
  LIST_ENTRY                Link;

  EFI_DEVICE_PATH_PROTOCOL  *DevicePath;

  CHAR16                    *OptionName;
  UINTN                     OptionNumber;
  UINT16                    BootCurrent;
  UINT32                    Attribute;
  CHAR16                    *Description;
  VOID                      *LoadOptions;
  UINT32                    LoadOptionsSize;
  CHAR16                    *StatusString;

} BDS_COMMON_OPTION;

typedef struct
{
	UINT32			Signature;
	LIST_ENTRY		Link;
	UINT32			PListSize;
	CHAR8*			PListData;
	UINT32			ModuleSize;
	CHAR8*			ModuleData;

}KEXT_FILE_DATA;

typedef struct
{
	UINT32			Signature;
	LIST_ENTRY		Link;
	CHAR8			DriverName[32];
	CHAR8*			DriverAddress;
	UINT32			DriverSize;

}DRIVER_DATA;
